build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - poetry run python booking_microservice/manage.py db upgrade
run:
  web: poetry run gunicorn -w 2 --bind 0.0.0.0:$PORT "booking_microservice.app:create_app()"
